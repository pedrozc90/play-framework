# Routes
GET         /                           controllers.Application.index()
GET         /health                     controllers.Application.health()
POST        /orders                     controllers.Application.order()
GET         /purchase-orders/:id        controllers.Application.getPurchaseOrder(id: Long)
GET         /orders/:id                 controllers.Application.getOrder(id: Long)
