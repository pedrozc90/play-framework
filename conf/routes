# Routes
GET         /                    @controllers.Application.index()

GET         /health              @controllers.health.HealthController.health()

POST        /api/files           @controllers.files.FileController.upload(req: Request)

GET         /assets/*file        @controllers.Assets.at(path = "/public", file)

->          /api/auth            auth.Routes
->          /api/users           users.Routes
->          /api/file            files.Routes
->          /api/v1/ext          externals.Routes
